# loot_reel_example

Example app for the `loot_reel` package.

## Run

```bash
flutter run
```

The demo shows a case-opening overlay flow powered by `LootReel`, including
weighted item generation and a custom tile builder.
